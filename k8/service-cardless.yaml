# cardless service load balancer
kind: Service
apiVersion: v1
metadata:
  name: cardless-service
spec:
  type: LoadBalancer
  ports:
    - port: 9090
      targetPort: 80
      protocol: TCP
  selector:
    app: cardless-service